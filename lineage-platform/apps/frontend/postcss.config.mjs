// Empty postcss config — Carbon handles all styling via Sass.
// This file exists to override any postcss config that PostCSS might
// auto-discover in a parent directory (e.g. ~/postcss.config.js).
export default { plugins: {} };
