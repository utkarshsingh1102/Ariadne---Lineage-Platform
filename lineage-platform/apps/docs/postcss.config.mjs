// Empty postcss config — Docusaurus handles CSS via its own pipeline.
// This file shadows the auto-discovered ~/postcss.config.js (which tries
// to load tailwindcss, a dependency this site does not use).
export default { plugins: {} };
