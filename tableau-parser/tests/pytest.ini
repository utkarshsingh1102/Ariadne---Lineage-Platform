[pytest]
testpaths = unit integration
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    neo4j: requires a running Neo4j (NEO4J_URI / NEO4J_USER / NEO4J_PASSWORD env vars)
    slow: marks tests that take >2s
addopts = -ra --strict-markers
