[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    neo4j: requires a running Neo4j instance (set NEO4J_URI / NEO4J_USER / NEO4J_PASSWORD)
    slow: takes more than ~2s
addopts = -ra --strict-markers
